import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.esteban',
  appName: 'capacitor-angular-novatec',
  webDir: 'dist/capacitor-angular-novatec',
  server: {
    androidScheme: 'https'
  }
};

export default config;
